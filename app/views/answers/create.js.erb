<% if @answer.errors.present? %>
  $('.answers .errors').html("<%= j render 'shared/errors', item: @answer %>");
<% else %>
  $('input#answer_body').val('');
  $('.answers>ul').append("<%= j render @answer %>");
<% end %>
